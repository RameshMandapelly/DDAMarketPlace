# ============================================================
# ADD THESE LINES to your existing .gitignore file
# Located at: solution root folder (same level as .sln file)
# ============================================================
 
# Never commit production secrets to Git
appsettings.Production.json
appsettings.UAT.json
 
# Never commit local developer secrets
appsettings.Development.json
 
# Keep only the base appsettings.json in Git
# (it should contain no real secrets — only placeholders)

# Environment-specific config files — contain secrets, never commit
appsettings.Production.json
appsettings.UAT.json